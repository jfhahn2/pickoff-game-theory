% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_game_model.R
\name{estimate_game_model}
\alias{estimate_game_model}
\title{Estimate game model}
\usage{
estimate_game_model(
  data,
  players = c("one", "two"),
  percentile_players = NULL,
  runner_percentile = NULL,
  battery_percentile = NULL
)
}
\arguments{
\item{data}{A data frame containing historical play-by-play data used to
estimate transitions.}

\item{players}{Character string. Specifies the game type: \code{"one"}
(Markov decision process) or \code{"two"} (stochastic zero-sum game).
Defaults to \code{"one"}.}

\item{percentile_players}{A list of player performance data frames (from
\code{extract_percentile_players}) used to set specific runner/battery
skill levels. Defaults to \code{NULL}.}

\item{runner_percentile}{Numeric. The percentile value to filter for the
runner's performance. Defaults to \code{NULL}.}

\item{battery_percentile}{Numeric. The percentile value to filter for the
battery's performance. Defaults to \code{NULL}.}
}
\value{
A data frame containing the optimal \code{policy_runner} and
the resulting state \code{value} for every game state in the transition model.
}
\description{
Estimates the full game model by first estimating transition probabilities
between states and then using value iteration to find the optimal policy.
}
